(()=>{'use strict';const ROOT='assets/badges/badge-sprite.svg';const rankCode={Curious:'curious',Observer:'observer',Experimenter:'experimenter',Agent:'agent',Associate:'associate',Investor:'investor',Mogul:'mogul','Billionaire Material':'billionaire_material',OWNER:'owner'};const esc=v=>String(v??'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]));const icon=rank=>{const code=rankCode[rank]||'member';return `<svg class="iwbr-badge-icon" aria-hidden="true" focusable="false"><use href="${ROOT}#${code}"/></svg>`};const normalize=v=>String(v||'').trim().replace(/^◆\s*/,'');const render=(el)=>{if(!el||el.dataset.badgeUi==='1')return;const raw=normalize(el.textContent);if(!rankCode[raw])return;el.dataset.badgeUi='1';el.classList.add('iwbr-rank-rendered');const owner=raw==='OWNER';el.innerHTML=`<span class="iwbr-inline-badge${owner?' is-owner':''}">${icon(raw)}<span>${esc(owner?'OWNER':raw)}</span></span>`};const scan=()=>{document.querySelectorAll('.forum-author-rank,.thread-author-rank').forEach(render);document.querySelectorAll('.leaderboard-identity > span').forEach(el=>{if(el.dataset.badgeUi==='1')return;const raw=el.textContent||'';const match=raw.match(/@[^·]+·\s*(Curious|Observer|Experimenter|Agent|Associate|Investor|Mogul|Billionaire Material|OWNER)/);if(!match)return;el.dataset.badgeUi='1';el.innerHTML=esc(raw.slice(0,match.index+match[0].indexOf(match[1])))+`<span class="leaderboard-rank-badge${match[1]==='OWNER'?' is-owner':''} iwbr-inline-badge">${icon(match[1])}<span>${esc(match[1])}</span></span>`+esc(raw.slice(match.index+match[0].indexOf(match[1])+match[1].length))})};const init=()=>{scan();new MutationObserver(scan).observe(document.body,{childList:true,subtree:true})};document.readyState==='loading'?document.addEventListener('DOMContentLoaded',init,{once:true}):init();window.IWBRBadges={rankCode,icon,scan}})();